# ========================================
# ПРИМЕР ФАЙЛА С ПЕРЕМЕННЫМИ ОКРУЖЕНИЯ
# ========================================
# 
# ИНСТРУКЦИЯ:
# 1. Скопируй этот файл и назови его .env
# 2. Заполни реальными значениями
# 3. НИКОГДА не коммить .env в Git!
#
# На сервере создай .env файл в папке проекта:
# /var/www/mysite-stage/.env (для stage)
# /var/www/mysite-prod/.env (для production)
# ========================================

# ========================================
# DJANGO НАСТРОЙКИ
# ========================================

# Какие настройки использовать (выбери одно):
DJANGO_SETTINGS_MODULE=config.settings.dev
# DJANGO_SETTINGS_MODULE=config.settings.stage
# DJANGO_SETTINGS_MODULE=config.settings.prod

# SECRET_KEY - секретный ключ Django (ОБЯЗАТЕЛЬНО!)
# Генерируй новый ключ для каждого окружения!
# Сгенерировать можно тут: https://djecrety.ir/
# Или командой: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=your-super-secret-key-here-change-me

# Разрешенные хосты (через запятую, без пробелов)
# Локально:
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Для stage:
# DJANGO_ALLOWED_HOSTS=stage.example.com,194.87.99.126

# Для production:
# DJANGO_ALLOWED_HOSTS=example.com,www.example.com

# DEBUG режим (True для dev/stage, False для prod)
DJANGO_DEBUG=True


# ========================================
# БАЗА ДАННЫХ
# ========================================

# PostgreSQL настройки (для stage и prod)
DB_NAME=mysite_db
DB_USER=mysite_user
DB_PASSWORD=super_secret_password_change_me
DB_HOST=localhost
DB_PORT=5432

# Для локальной разработки SQLite используется автоматически


# ========================================
# EMAIL НАСТРОЙКИ (для production)
# ========================================

# Пример для Gmail:
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password


# ========================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ (опционально)
# ========================================

# Redis для кеширования (если используешь)
# REDIS_URL=redis://127.0.0.1:6379/1

# Sentry для мониторинга ошибок (если используешь)
# SENTRY_DSN=https://your-sentry-dsn-here
